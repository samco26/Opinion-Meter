# Handoff — 15 September 2026

Where Opinion Meter is up to, for whoever (or whatever) picks it up next. README.md is the specification; AGENTS.md the rules; CHANGELOG.md the version history; this is the state.

## v0.16 — 16 September 2026: the design handoff, and a card that grows without moving

`extension/src/ui.ts` was rewritten again. Shapes (`createBar({ shape })`): `block` (per result: the verdict beside Google's name, a 2 px bar under it across the column, `--om-indent`/`--om-block`/`--om-line` from `bar.name()` and a 5 px `[data-opinion-meter="gap"]` inserted after the name's line by `place()`), `story` (34 px bar + verdict after a panel entry's name; `data-bare` drops the verdict when the dots are close), `line` (the query line: "What people think of X", bar flex, verdict), `square` (beside a knowledge panel title), `site` (the badge). The grown card is `.card`, absolutely positioned inside the host at `-padding` so the header never moves; its content (dots, summary, tiles, chip, meta, divider, footer) is drawn from the gauge alone, no fetch; "See recurring opinions" frames the server's page with `part=opinions` in `.sheet`; tiles and "How it works" open the page in a new tab with `view=`. Hover opens after 300 ms and closes 200 ms after leaving; a click pins; a pinned card closes on ×, Escape, outside click or any plain click; `hoverArmed` stops a reopen under the pointer after a close. Tokens per the handoff for light and dark hosts (`:host([data-dark])`); the host's font is read from the page. `site.ts` docks the badge under a fixed/sticky header (`headerBottom()`), no remembered drag spot. Server: `Embed` gains `part="opinions"` (list only, flush) and `start` (a platform or "how" when opened in a tab); `OpinionPills` rows carry a `.tick`; globals.css ends with the handoff's row styles. `tests/bundle-preview.mjs` builds hands/site/embed bundles for a standalone test page (CI artifact `opinion-meter-preview`); an artifact page cannot frame the server (CSP) but can frame a blob page it builds, which is how the owner's test page works. **Not seen running** when written; the owner tests it on the artifact page before it ships.

**17 September, on the branch before it merged (owner's test page, then owner's tweaks).** (1) Geometry of the morph, the part that was "still broken": `grow(from)` takes the card's start size *before* the state flips (`setState` measures first), lays the contents out at their final width from the first frame (`body.style.width`, `head.style.width`), and both the opening's first box and the closing's last box are the bar wrapped in the card's padding plus the sideways shift and a story's name line (`wrapped()`), so the bar is never clipped by the card's edge; the card's width is at least the bar's own width plus padding (long addresses). A `.closing` class keeps padding, name line and body (fading) while the box shrinks; `relax()` (transitionend or 400 ms) strips the inline sizes and puts a lifted query line back. The badge's padding and `left` animate too. When `state !== "rest" || closing` the ResizeObserver leaves the host alone; the query line's host keeps its own width (only the height is mirrored) and its card spans the host (`:host([data-shape="line"]) .card{width:calc(100% + 2px)}`). (2) Owner's placement tweak: the per-result bar starts at the result's left edge (the block's, level with the title and favicon) and ends at the column's end; the host sits at the row's exact fractional position (no rounding) so the verdict shares the name's pixel row; the verdict sits 6 px after the name. The head grid gained a `lead` column (`--om-lead`, the favicon's room) and `bar.name()` takes `lead` and the favicon element; `lookalike()` clones it with its computed look written inline (`LOOK`) so the grown card shows the favicon in place. (3) Test tooling that works without Node: `scratchpad/serve.ps1` (PowerShell HttpListener) serves the assembled test page to the in-app Browser pane; when that pane is hidden its animation clock stops (`getAnimations()` all at 0) and ResizeObservers never fire (hosts 0×0), so step transitions by hand (`a.pause(); a.currentTime = t`) or test in the owner's Chrome on `http://localhost:8765/`; `ci-fetch.sh <full sha> <branch>` waits for CI and downloads `opinion-meter-preview`.

**v0.16.1 (17 September, from the owner's first test of the shipped build).** Result bars show from creation (`show = s.kind !== "empty" || thin`), sweeping until the reading lands. The resting `.card` has `border-radius:0` (the 12 px corners with `overflow:hidden` clipped the bar's ends; the radius comes with hover/open/closing). The badge no longer gets `host.style.zIndex = "1"` when grown (that inline value beat `:host([data-site])`'s 2147483000 and put the card under Wikipedia's header at z 3). On Google the favicon is a sibling of the name-and-address column, so `place()` now takes the row's left edge from the title (`Found.heading`) and `google.ts favicon()` finds the favicon by its `img` on the name's line, picking the wrapper the circle is drawn on; `bar.name(text, indent, more)` takes `{ block, line, lead, icon, iconX, iconY, like }` — the copy is placed absolutely at (iconX, iconY) inside the head, and `like` (the page's name span) lends its computed font to the `.name` overlay so the name does not change size when the card opens. `fitBar()` makes `--om-block` at least the name-and-verdict row's width. Tiles always list YouTube, X, HN and Bluesky (Reddit once it has posts); `.card.list .actions{margin-top:14px}`; the embed's list-only loading strip is a 3 px hairline sweep (`globals.css`, end).

**v0.16.2 (17 September, the owner's second test).** The owner's red line: the bar starts at the name-and-address column's left (`left = min(name.left, address.left)` in `place()`), not under the favicon; the favicon's room becomes the card's *lead* (`--om-lead`, read by `leadNow()` in `grow()`): the grown card's left edge moves `lead` further left, `head.style.marginLeft = shift + lead` keeps the row put, `body.style.marginLeft = lead` starts the figures, summary, tiles and list level with the name (the favicon copy sits at `--om-icon-x`, negative, absolutely inside the head), the card is `CARD_WIDTH + lead` wide and `wrapped()` counts the lead. `content.js` runs at `document_end` so bars sweep as the results appear. List mode keeps the dots and summary (`.card.list` only reveals heading and sheet); the sheet's floor is 40 px. Platform tiles and "How it works" post `{ om, type: "view", view }` to the framed page (`openView()`; sent on "ready" if the list was just opened) and `Embed` listens (`viewNamed`), showing `PlatformEvidence` or the explainer with "← Back" (`.embed.list-only .back-button`). `site.ts` hears `scroll` in capture on the document and judges by the scrolled box (`scrollTop`), since many sites scroll a box rather than the window.

**v0.16.3 (17 September, the owner's third test).** `content.js` runs at `document_start`: `main()` waits for `document.body`, scans at once, and the MutationObserver scans on the leading edge (a look when the page changes after 400 ms of quiet, another 250 ms later while `readyState === "loading"`, 500 ms after). `organic()` skips the last `[data-hveid]` element while the page streams (it may be half parsed; `known` would otherwise freeze a wrong placement). `positionQuery()` remakes the query bar in the other shape when its placement changes (`remakeQuery`, keeping the key and `queryState`); `scan()` rechecks `isDark()` and retoggles `data-dark`. Ads: `siteLabel()` falls back to `addressLeaf()` (the lowest small leaf reading like an address above the title) when there is no `cite`, and the container also matches `[data-text-ad], [data-dtld]`. The query line has a `.tagline` (the gauge's sentence) under the bar at rest; grown, the body's summary is hidden for that shape and the dots follow the tagline; the host mirrors the card's rest size (head + tagline) via a ResizeObserver on head and card. The mock page has a sponsored fixture (`#tads`, span address).

**v0.16.4 (17 September).** `positionQuery()` (flow) and `liftQuery()`'s restore clear only `left/top/width/visibility` — a wholesale `cssText = ""` had wiped the ResizeObserver-mirrored `height` (so the tagline overran the first result) and `--om-font`. `main()` reads the brain's cached config from `chrome.storage.local` (`storage.get("config")`, checked against `serverUrl()` and a day's age) and scans on it at once; the brain's `config` answer is awaited in parallel and takes over, or tears the bars down (`off`) if the meter was switched off. Hosts carry `data-om-at` (page ms at creation) for timing audits.

**v0.16.5 (17 September).** Latency: `gaugeFor` waits `QUICK_MS` (2.5 s) per fresh reading instead of the whole 20 s budget (slow ones go pending; the hands poll, `pollMs` 3000 × `polls` 10). `resultSubjects(query, results, timeout, background?)`: with `background` (the gauge route passes `keepAlive`), names come from memory only (`cachedQueryName`, `cachedSiteNames` in analysis/name.ts); a query or site never named is started in the background and reported as `later` (`GaugeResponse.query.later`, `results[].later` with `key: null`). The brain does not `keep` later names; the hands (`drain`) keep those bars sweeping (`retrying` set, the bar is reused on the next pass) and re-ask after `RETRY_MS` 1500 (`queryAgain` for a query-only re-ask; the drain loop runs on it). `RATE_PER_TOKEN_MINUTE` default 60. Card: `.dots` moved out of the body to sit between the head and the tagline, folded at rest (`max-height:0; width:0`), sliding open in hover/open and folding in `.closing`; the line shape keeps its tagline as the summary (`.body .summary` hidden, `.body{padding-top:18px}`).

## v0.15 — 16 September 2026: one card that grows

The drawer (`openOverlay`) and the tooltip are gone from ui.ts. `createBar` now builds one growing element per bar: `.card` (background, radius, frosted when grown) holding the row (`.bar`), the figures under the bar (`.figs`, aligned to the row's seg by measurement), the sentence (`.peek`, hover) and the sheet (`.sheet`, the embed iframe, open). `grow()` animates width and height with the auto-height trick, shifts the card left when the window's edge is near (the row is pushed right by the same amount so it stays put), grows upward (`.card.up`, reversed order, translateY) when there is more room above than below, and caps the sheet to what fits. States: rest, hover, open (`bar.state()`); Escape, the row's ×, a click outside or the embed's close message shrink it. content.ts: `drawerFor()` builds the embed address with `morph=1`; `liftQuery()` moves the flow pill onto the layer with a blank in its place while open; `pinBar` skips clipping and `positionQuery`/`pinQuery` stand still while open. site.ts: scrolling closes an open card; `place()` and the ResizeObserver stand still while grown. The embed in morph mode drops its title bar and its own bar and figures (`morph` prop, `Answer bar={false}`). The flow pill's host is `position:relative` with the card absolute, so a hover never pushes the results. The site pill's bar column is `minmax(48px,1fr)` at `width:100%`, so it fills the pill. **Not seen running**: everything in this section (written after the owner approved the interactive mock-up, the artifact "Opinion Meter Morph"). If Google's results container clips the hover card of the flow pill, lift on hover as well as on open.

## v0.13 — 16 September 2026: drag, frosted card, Edge and Safari

(1) Dragging: pointer events on the site bar in `createBar` (capture, 4 px threshold, `data-dragging`, the click after a drag suppressed); `onMove` stores `sitePos` in local storage and `site.ts` restores it clamped to the window, re-clamping on resize. (2) The site card's `.big` takes the drawer's frosted look (`#f3f5f6d9` / `#303134d9`, blur, 1 px border) and bold name/verdict. (3) `build.mjs` targets chrome, edge, firefox, safari; ci.yml uploads all four folders and has a gated `safari-app` job on `macos-latest` running `xcrun safari-web-extension-converter` and `xcodebuild` unsigned, zipping "Opinion Meter.app" as `opinion-meter-safari-app`. Gate: `workflow_dispatch` or "[safari]" in the push message. `SAFARI.md` at the root is the owner's step-by-step for a Mac (use the CI app, or build in Xcode; Develop menu → Allow Unsigned Extensions; signing later), with Edge and Firefox loading notes. The menu's server-address field is gone (the owner's decision): to test against a preview server, set the `server` key in the extension's local storage from the service-worker console and remove the `config` key. **Not seen running**: the Mac job (converter flags and the scheme lookup are from Apple's documentation; the first run tells), dragging, the frosted card. The v0.13 push carries "[safari]" so the first Mac run happens; read its log if it fails (scheme name, converter warnings).

## v0.12 — 16 September 2026 (small hours)

Built from the owner's first look at v0.11. (1) **On by default** — the owner chose the install-time way: `content_scripts` now carries `*://*/*` (excluding the Google search patterns) for `site.js`, Firefox lists `host_permissions: ["*://*/*"]` (min 127, which asks at install); the optional-permission code, `scripting`, the one-time note (`showOffer`) and the `offer`/`sites` messages are gone. The menu's switch stores `sitesOff` in local storage; the brain's `lookup()` answers nothing while it is set. (2) **Popup** (`popup.html/.ts`, `action.default_popup`; `options_ui` points at the same page with `open_in_tab: true`) replaces `options.html/.ts`; the embedded settings dialog resized itself endlessly (its height changed as text arrived, a scrollbar appeared, the width changed, and so on). (3) **Scroll fade**: `site.ts` toggles `data-faint` on any scroll; `:host([data-site][data-faint]){opacity:.22}` with hover back to 1. (4) **Drawers under the header**: `headerLevel()` moved to ui.ts (undefined off Google); `openOverlay({ level })` sets the panel's z-index to it and the backdrop one under. (5) **Dark text**: `[data-theme="dark"]{color:var(--ink)}` — the override of `--ink` on `.embed` did not reach text that inherited the body's colour. Not seen running: the popup, the fade, the install-time permission on a packed install.

## v0.11 — 15 September 2026 (night): take the bar with you

The first feature beyond Google's pages. Pieces: `extension/src/site.ts` (the site script: asks the brain by address, draws the fixed 224 px pill with a ×, opens a fixed drawer); `background.ts` (files readings under `host:`, `site:` and `page:` keys in `chrome.storage.session` when a result's bar is ready — `carry()`, `carryAll()`, `waiting` for subjects still pending — answers `site` by page, then host, then site, honouring `site-hide` times; holds the `offered` flag; registers/unregisters the site script with `chrome.scripting` when the settings say so and on startup if the permission is held; the toolbar icon opens the settings); `options.ts/html` (the Turn on/off button asks the browser for `*://*/*` on a click, then tells the brain); `build.mjs` (`scripting` permission, `optional_host_permissions` for Chrome, both optional keys for Firefox, the `site` bundle); `ui.ts` (`data-site` styles, `onDismiss`, `showOffer`, `fixed` drawers, `isDark` shared); `content.ts` (the one-time note after the first reading when `reply.sites.offer`). The privacy page and README §9 describe it in the same words. Not seen running: the permission prompt, the dynamic registration, the pill on a site — all written from the API documentation. Firefox: `optional_host_permissions` may be unknown to older versions (a manifest warning); `optional_permissions` carries the hosts there.

## v0.10.1 — 15 September 2026 (night, fixes)

(1) **The drawer stayed white in dark mode** although its text went light: `.panel.dark iframe{color-scheme:dark}` gave the frame a different colour scheme from the page inside it (`html{color-scheme:light}`), and Chrome then paints an opaque canvas behind an iframe whose scheme differs from its document. Rule: the frame and the embed keep the same scheme (light); the dark look comes from our own colours. (2) Solid two-block loading sweep. (3) Duplicate bars in the expanded sources panel: Google keeps the collapsed copy of the list in place but unseen; `shown()` now runs `checkVisibility` at every tick (visibility and opacity), `clipTo()` trims by every clipping ancestor (`window_()`), and `organic()` skips links inside the panel. Not reproduced here: the assistant's page shows "Show all", which opens nothing in place; the owner's shows "Show more", which expands into an inner scroll box.

## v0.10 — 15 September 2026 (night)

Built from the owner's tests of v0.9 (kia Videos tab, peptides AI Overview after "Show more", the youtube knowledge panel). See CHANGELOG.md. Findings: (1) **after "Show more" the sources panel was lost** because `sourcesPanel()` looked for a control reading "Show all", and a pressed control reads "Show less"; the query card above it then fell into the flow and the newly shown sources got no bars. (2) The expanded panel scrolls inside itself; bars pinned by page coordinates lagged half a second and drifted outside it — `content.ts` now repositions on any inner scroll (capture-phase listener, page scroll excluded) and `clipTo()` trims each bar at its scroll box's edges (`scrollerOf()`). (3) The one look: the query card's pill (Google Sans, #1f1f1f on #f3f5f6; #e8eaed on #303134 in dark) now drives the result bars, the hover card (`.tip.dark`) and the drawer (`.panel.dark`, the embed's `data-theme="dark"` from `?theme=dark`, overrides at the end of globals.css). Google Sans is loaded in `layout.tsx` from Google's font service with Arial as the fallback; whether Google serves it to our origin is not verified. (4) "How it works" is back at the bottom (the owner reversed v0.9's move). (5) The knowledge-panel card's bar sits on the subtitle line: `kpHeader()` returns `base` (the subtitle's text box) and `pinQuery` offsets by the bar's position inside the card.

## v0.9 — 15 September 2026 (late evening)

Built from the owner's tests of v0.8 (kia All tab, Shopping, AI Mode, the App Store drawer). See CHANGELOG.md. Findings: (1) **bars sat far right of the site name** because Google's name span runs the width of its block — `pinBar` now measures the text itself (`textBox`). (2) **Shopping tiles had no bars** because the seller line ("21Overlays", "evee") rarely looks like a web address; the tile is now found from its price and the seller is the first small line under it that is not a delivery/returns note, a rating or a price (`PRICE`, `TILE_NOTE` in google.ts). (3) **AI Mode fades its answer in**, so v0.8's opacity check skipped everything on a scan that landed mid-fade; the check now ignores opacity (visibility and fixed boxes still count). (4) The drawer is anchored to the page (`.panel{position:absolute}` at page coordinates taken when it opens); the "Opening the drawer…" cover is gone; close fades. (5) The brain keeps gauge answers ten minutes (`background.ts`: `resultKeys`, `queryKeys`, `readings`, `prepared`), so tab changes within one search draw from memory. Also: the knowledge-panel card sits right after the dots with the verdict beside the bar (grid layout, `QUERY_SQUARE` 160); "Mixed opinion"; the query card mirrors Google's pill button (46 px, 30 px radius, 14 px Google Sans); "Lately" is 14 days and ≥3 refs; the "How it works" link moved under the count; the loading phrase fades every 3.2 s.

Not seen drawn: the corner placement for AI cards (the owner's "news card" was not identified for certain — it is either the AI Mode quick-result cards, which have no heading element and are now read by their first large line, or a sources-panel entry whose name sits low), the anchored drawer, the Shopping seller rule on the owner's page.

## v0.8 — 15 September 2026 (evening)

Built from the owner's tests of v0.7 (peptides with an AI Overview, youtube with a knowledge panel, kia, the App Store drawer). See CHANGELOG.md. Findings: (1) **the "phantom bar" that followed the reader down the kia page was a bar for Google's hidden "My Ad Centre" link** — a pop-up built into the page, kept unseen, in a box fixed to the window; the hands counted it as a result because it has a size. Now anything invisible by style, inside a fixed box, inside a menu/dialog, on Google's own service hosts, or titled like a control is skipped (`seen()`, `POPUP`, `SERVICE_HOST`, `NOT_A_TITLE` in google.ts). It looked filled grey because thin "no verdict" bars lost their outline style in dark mode (fixed in ui.ts). (2) **AI Overview source bars sat outside the box** because Google clips each label in a 200 px block while the label's text runs on unseen past the dots; `pinBar` now takes the nearest clipping ancestor's edge as the visible end, one width (60 px) for all. (3) **The knowledge-panel card fell onto the image** because it measured the full-width title block; `kpHeader()` measures the text itself plus the dots, treats a logo or thumbnail on those lines as a limit, and `queryPlacement` falls through to the flow when the card would not fit. (4) Bars painted over Google's floating search header: the layer's stacking level is now one under `#searchform`'s (128 → 127), read live. Also: no loading words on the query card; stars gone; the drawer's card never scrolls (flex column, the opinions list gives way to a 120 px minimum); the loading drawer is a ~75 px strip; X's note deduplicated.

X still reads nothing: the live card says "Your monthly spend cap has been reached" for both searches — the owner raises the cap in the X developer portal (Billing); it resets on 1 October otherwise.

## v0.7 — 15 September 2026 (afternoon, third pass)

CI run #23 green; server 0.7.0 live. See CHANGELOG.md for the list. The two findings that matter: (1) **the bar and the drawer disagreed because the bar rested on a 19-opinion quick pass** — now the bar reads the same three years, platforms and 250-entry sample as the card, the card writes its numbers back into the gauge (`rememberGaugeFromCard` in gauge.ts), and the embed posts a `gauge` message to the drawer host so the bar under it updates on the spot; live check: Reddit bar 51/0/49, card 51/0/49. (2) **X is refused because the developer console's monthly spend cap is reached** — the note now carries X's own words; the owner raises the cap in the X developer portal (Billing). The 7-day fallback is in place for tokens without archive access. Also: Shopping tiles sit in an aria-hidden block (visibility check relaxed); AI answer sources get a bare bar shrunk to fit before the dots; sites outside the table are named by the AI per host (`nameSites`); no prefetch on Videos tabs; thin bars hover-only; drawer relaid out (full-width bar, gold stars + icons left, count right, opinions list scrolls on its own, minimal loading state with rotating phrases).

Still open with the owner: the unfinished "also these clickable recurring op."; whether to keep the query card on the Videos tabs (kept, no prefetch); raising X_MAX_RESULTS above 20.

## v0.6 — what changed on 15 September 2026 (afternoon, second pass)

Built from the owner's live tests of v0.4/v0.5 across the All, Images, Videos, News, Shopping, AI Mode and AI Overview pages.

**Bars on a layer.** Bars are no longer inserted into Google's DOM. One layer (`[data-opinion-meter="layer"]`, absolute, at the document root) holds every bar, and each bar is pinned by page coordinates to a *target* element, re-pinned every 500 ms and on resize (`content.ts` `pinBar`/`reposition`). Targets by page (`google.ts`): the "About this result" dots on All, Forums and Videos (bar to their right, same line as the address); the source name on a News card; the merchant line under a Shopping tile (bar below it); the source label inside an AI Overview / AI Mode sources panel (found from its "Show all" control). Nothing of Google's can clip a bar or its glow any more.

**The query's card.** Beside the knowledge panel's logo, title and subtitle on the same line (`kp` mode; under them only if the panel is too narrow); above an AI Overview's sources box at the box's width; below an AI Mode sources panel (`udm=50`); otherwise in the flow above the results as one line (title · bar · percentage).

**Fixes.** Google Play results were being dropped because every `google.*` host was treated as a search redirect — now only the search host is. A YouTube video (Videos / Short videos tabs, or any video link) is read from its own comments alone: no search, no other platforms (`sourcesFor`, `youtube.ts`). Shopping sellers without an address get a stand-in host (`merchant.invalid/<slug>`) and their label ("& more", "AU" stripped) so `siteSubject` can name them.

**Look.** Greyscale everywhere except the three sentiment colours, brightened (`--sentPos #3fae66`, `--sentNeg #d95d52`). Icon-only platform buttons; recurring opinions glow on hover, no arrow; the drawer's title is 34 px and aligned with the text; the coverage line ("YouTube returned nothing…") and the opinion count in the tooltip are gone; steady glow on hover. Version 0.6.0 on both halves (CI run #21 green; server live).

## Not yet verified (in order of risk)

1. **v0.8 on any live page** — written from DOM inspections through the owner's Chrome (peptides AI Overview sources box, youtube knowledge panel, kia results), not seen drawn. Watch: bars beside the site name on the All tab; the kia page without the phantom; the AI Overview bars inside their box; the knowledge-panel card on the title lines.
2. **The header layering** — Google's floating search header never appeared in the assistant's test window, so "bars slide under the header" is reasoned from `#searchform`'s stacking level (128), not seen. If any Google box now covers a bar, the fallback is clipping the layer at the header's bottom edge instead.
3. **The drawer without a card scrollbar** — the flex layout and the height the embed asks for (list unfolded) were not run locally (no Node); CI builds it, the owner sees it.
4. **AI Mode** (`udm=50`) still uninspected (the connector refuses scripts on its tokenised URL).
5. **YouTube** quota status after the reset (5 pm Sydney).
6. **Open with the owner**: the unfinished sentence "also these clickable recurring op."; raising X_MAX_RESULTS above 20 once the cap is lifted.

## Immediate next actions

1. **Load the newest package**: the assistant unpacks the newest green run's `opinion-meter-chrome` over `extension/dist/chrome` (v0.9 once its run is green) — the owner presses Reload at `chrome://extensions`. The owner wants a written summary of every change before a push. For later builds the assistant fetches the artifact itself: `git credential fill` yields the stored GitHub token (never printed), the Actions API gives the newest run's `opinion-meter-chrome` artifact id, `curl -L` on `/actions/artifacts/<id>/zip` downloads it, PowerShell `Expand-Archive -Force` unpacks it, and the manifest version plus a build marker in `content.js` confirm the swap. No more manual downloads.
2. **Walk the pages**: "kia" (All: bars beside the names, no phantom, scroll under the header), "youtube" (knowledge panel card on the title lines), "peptides" (AI Overview: bars inside the box, the narrow card's name), "twitter" (News), a drawer (no card scrollbar, short loading strip, no stars).
3. **X monthly spend cap** — raise it in the X developer portal (Billing); until then X reads nothing. **YouTube quota increase** — still urgent.
4. Reddit stays parked.

## How the pieces talk (one paragraph)

The hands read the page — titled results, news cards, shopping tiles, an AI answer's sources — with each result's address, title, and the site label Google prints beside it, plus the query; the brain POSTs them to `/api/gauge` with the install token. The server names each result's site (the sites table, the label, or the title; a YouTube video by its id) and the query's subject (one AI call, with aliases), merges duplicates, answers from memory or computes a lite gauge over three years within 20 seconds, and keeps unfinished work alive for the extension to poll (`GET /api/gauge?keys=`). The brain then asks `/api/card?key=` for the query's card so it is ready. Each bar is drawn on the layer and pinned beside its target once its reading exists (an empty outline when too thin); the query's card is pinned beside the knowledge panel's title, above or below a sources panel, or placed in the flow. Clicking a bar opens the drawer: an overlay framing `/embed?key=…`, which fetches `/api/card?key=` (held 15 minutes) and draws the card, the "Lately" line, opinions and evidence. `/api/config` is read on start; the memory key `config:override` changes it instantly (kill switch, selectors).

## Gotchas learned

- Bash heredocs over ~8 KB fail on this Windows machine (command-length limit); large files are written with the file tool. Perl is available for small in-place edits; use `\Q…\E` around literal text.
- The in-app browser gets a Google CAPTCHA; the owner's own Chrome (via the Chrome connector) renders Google with the extension's bars, runs JavaScript for DOM inspection (not on AI Mode's tokenised URLs) and reads the private repo's Actions pages and logs (open the job URL with `#step:N` and read the page text). It cannot open `chrome://` pages; clicking an artifact download link only works while the owner is not using Chrome — otherwise the owner downloads it (two clicks; from the *newest green* run) and the assistant unpacks it from Downloads. The owner may press Reload before the unpack — check the loaded build (`querySquare`/layer presence) before diagnosing.
- Google's result containers carry CSS transforms and the results column clips: nothing of ours goes inside them any more; the layer at the document root is the only place bars live.
- Google's "About this result" dots sit outside the result's anchor, on the address line, on All, Forums and Videos; News has no dots and no `cite`; Shopping tiles have no link element; AI answer sources are `li` entries whose overlay link has no text, with the label clipped by a 200 px block (`overflow:hidden`) while the label span itself runs on.
- Google keeps pop-ups built into the page but unseen (the "My Ad Centre" link, "About this result" dialogs): they have a size, so `getClientRects()` alone does not mean visible — use `checkVisibility` and refuse anything under a `position:fixed` ancestor.
- The Chrome connector: scripts in a background tab stall (timers are throttled; 45 s timeouts) — run them on the active tab; on Google's tokenised URLs a result that looks like query-string data (`key=value` text) is blocked, so return compact JSON; a tab the connector opens may be moved out of its group by the owner.
- `SEARCH_ENGINES` must be checked after the sites table (Google Play is on google.com); every `SubjectKind` needs a label in `CategoryCard.tsx`; `querySelectorAll<HTMLElement>("a[href]")` has no `.href` for the type checker — use `HTMLAnchorElement`.
- Git's housekeeping in the OneDrive-synced repo makes OneDrive ask about deleting hundreds of `.git/objects` files; it is safe.
- The office network answers requests to `public.api.bsky.app` with a proxy 403 page; probe Bluesky only through the server's doors.
- Line endings are forced to LF by `.gitattributes`; the CRLF warnings on commit are noise.
- The old site (`Desktop/t`, what-are-people-saying.vercel.app) is untouched and still has its own pending Reddit application, whose limits do not apply to this project.
