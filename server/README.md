# server

The always-on half. A Next.js project deployed to Vercel.

Planned doors: `/api/gauge` (a list of Google results in, quick gauges out), `/api/card` (one subject in, the full card out), `/api/config` (the settings file with the kill switch), and `/embed` (the card page the drawer shows). Plus the memory (Upstash Redis), the nightly cron, and `golden/` with the hand-checked subjects.

Not started. See README.md at the root, section 14, step 1.
