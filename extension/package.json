{
  "name": "opinion-meter-extension",
  "private": true,
  "version": "0.16.10",
  "description": "Crowd opinion before you visit. Opinion Meter reads public posts and shows a sentiment bar for every page.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts"
  },
  "devDependencies": {
    "esbuild": ">=0.24.0 <1.0.0",
    "typescript": "^5.6.2"
  }
}
