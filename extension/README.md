# extension

The installed half. A WXT project (Manifest V3; Chrome first, Firefox from the same build).

Planned parts: the label (manifest), the hands (a Google results skill in v1), the brain (talks to the server's doors), the drawer (side panel showing `/embed`), icons. No keys, no prompts, no analysis here, ever.

Not started. See README.md at the root, section 14, step 2.
